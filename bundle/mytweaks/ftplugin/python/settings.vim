" Use the below highlight group when displaying bad whitespace is
" desired.highlight 
"BadWhitespace ctermbg=red guibg=red 
" Use UNIX (\n) line endings. Only used for new files so as to not force
" existing files to change their line endings.
setlocal fileformat=unix
setlocal foldmethod=indent 
"Fold Settings
setlocal tabstop=2
setlocal shiftwidth=2
